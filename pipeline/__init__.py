"""`pipeline` — crowd-cast screen recordings → computer-use SFT records.

Each ``stage_NN_*.py`` is one runnable stage (00 clip manifest → 06 training
records), shared code lives in ``pipeline.lib``, and the per-method annotation
subsystem lives in ``pipeline.annotation``.
"""
