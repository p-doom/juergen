"""Realigned pipeline — crowd-cast screen recordings → computer-use SFT records.

Flat, stage-numbered successor to the annotation_pipeline / realignment_fix
split. Each ``stage_NN_*.py`` is one runnable stage (00 clip manifest → 06
training records); shared code lives in ``realigned_pipeline.lib``.
"""
