# Pipeline outputs (frame caches, runs, SFT samples) — large, regenerable.
outputs/

# Full-dataset run outputs (frame array_records, per-model clips, SFT) — GBs, regenerable.
dataset_runs/

# Iteration runs (frame renders, run artifacts) — large, regenerable.
iteration_runs/
iteration_clips.json

# Generated by discover_clips.py (regenerable from the upload tree).
clips_dataset.json
discovery_report.json

# Virtual envs and caches.
.venv/
__pycache__/
*.pyc
logs/
